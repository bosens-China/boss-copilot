// src/utils/discrete-api.ts
import { createDiscreteApi } from 'naive-ui';

export const {
  message,
  // notification, dialog, loadingBar
} = createDiscreteApi([
  'message',

  // 'notification', 'dialog', 'loadingBar'
]);
